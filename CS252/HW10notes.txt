Static IP address:

ifconfig p7p1 [ipaddress] netmask [netmask]

Static IP address (works through reboot)

vi /etc/sysconfig/network-scripts/ifcfg-p1p1

ONBOOT=yes
IPADDR=[ip address]
NETMASK=[netmask]


Samba Service Configuration:

/etc/samba/smb.conf

Add Samba user:

smbpasswd -a [user]